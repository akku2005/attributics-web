export const typography = {
    title: {
        SM: {
            fontSize: "clamp(1.2rem, 1.4rem + 2.4vw, 1.6rem)",
            fontWeight: 600,
            lineHeight: 1.1
        },
        MD: {
            fontSize: "clamp(2.0rem, 1.35rem + 2.4vw, 2.4rem)",
            fontWeight: 600,
            lineHeight: 1.1
        },
        LG: {
            fontSize: "clamp(2.4rem, 1.35rem + 2.4vw, 3.2rem)",
            fontWeight: 600,
            lineHeight: 1.1
        },
        XXL: {
            fontSize: "clamp(2.4rem, 1.35rem + 2.4vw, 4.2rem)",
            fontWeight: 600,
            lineHeight: 1.2
        },
        MAX: {
            fontSize: "clamp(2.4rem, 2.4rem + 2.4vw, 5.2rem)",
            fontWeight: 600,
            lineHeight: 1.2
        }
    },
    desc: {
        Normal: {
            fontSize: "clamp(1.2rem, 1.2rem + 2.4vw, 1.4rem)",
            fontWeight: 400,
            lineHeight: 1.4
        },
        Bold: {
            fontSize: "clamp(1.1rem, 1.1rem + 2.4vw, 1.2rem)",
            fontWeight: 600,
            lineHeight: 1.4
        },
        Small: {
            fontSize: "clamp(1.1rem, 1.1rem + 2.4vw, 1.2rem)",
            fontWeight: 400,
            lineHeight: 1.4
        }
    },
};